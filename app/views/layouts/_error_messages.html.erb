<% if model.errors.any? %>
  <div id="error_explanation" class="alert alert-warning">
    <h4 class="d-flex">エラーがあります。</h4>
    <ul class="mb-0">
      <% model.errors.full_messages.each do |message| %>
        <li class="d-flex"><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>