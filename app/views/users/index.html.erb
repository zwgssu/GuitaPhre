<% @page_title = "ユーザー一覧" %>
<div class="bg-white p-3">
<h1 class="text-center mb-1"><%= @page_title %></h1>
  <%= render "layouts/user_list", users: @users %>
  <div class="mt-3">
  <%= paginate @users %>
  </div>
</div>