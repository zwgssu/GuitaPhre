<% @page_title = "テーマ一覧" %>

<div class="bg-white p-3">
  <h2 class="text-center"><%= @page_title %></h2>
  
  <%= render "themes", themes: @themes %>
  <div class="mt-3">
  <%= paginate @themes %>
  </div>
</div>