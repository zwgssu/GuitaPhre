<% if current_user.liking_phrase?(phrase) %>
  <%= link_to "good", favorite_phrases_path(phrase_id: phrase.id), method: :delete, remote: true, class: "btn btn-primary" %>
<% else %>
  <%= link_to "good", favorite_phrases_path(phrase_id: phrase.id), method: :post, remote: true, class: "btn btn-outline-primary" %>
<% end%>
(<%= phrase.likers.count %>)